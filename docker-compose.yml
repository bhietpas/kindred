services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: kindred
      POSTGRES_USER: kindred
      POSTGRES_PASSWORD: kindred
    ports:
      - "5432:5432"
    volumes:
      - kindred-pgdata:/var/lib/postgresql/data

volumes:
  kindred-pgdata:
