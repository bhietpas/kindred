spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kindred
    username: kindred
    password: kindred
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health
